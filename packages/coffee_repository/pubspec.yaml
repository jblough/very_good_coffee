name: coffee_repository
description: Package to wrap interaction with the coffee API package
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  coffee_api:
    path: ../coffee_api
  local_storage:
    path: ../local_storage
  rxdart: ^0.28.0

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.14
  very_good_analysis: ^7.0.0
